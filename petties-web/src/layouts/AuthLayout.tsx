import { Outlet } from 'react-router-dom'

/**
 * AuthLayout - Simple wrapper for auth pages
 * Auth pages now have their own full-page design with brutalism style
 */
export function AuthLayout() {
  return <Outlet />
}

