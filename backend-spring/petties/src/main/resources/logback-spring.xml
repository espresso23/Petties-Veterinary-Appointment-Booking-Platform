<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <!-- Property để đọc từ Spring profiles -->
    <springProperty scope="context" name="appName" source="spring.application.name"
        defaultValue="petties" />
    <springProperty scope="context" name="sentryDsn" source="sentry.dsn" defaultValue="" />

    <!-- Console Appender với màu sắc -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) -
                %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File Appender với Rolling Policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/petties-backend.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>logs/petties-backend.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- Max file size before rolling -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 30 days of history -->
            <maxHistory>30</maxHistory>
            <!-- Total size cap for all logs -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Error File Appender - Chỉ lưu ERROR và WARN -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/petties-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n%ex{full}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/petties-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- JSON Appender cho structured logging (production) -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/petties-json.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>
                {"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}","level":"%level","logger":"%logger","thread":"%thread","message":"%msg","exception":"%ex{short}"}%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/petties-json.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Async Appender để không block main thread -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="FILE" />
    </appender>

    <!-- ============================================ -->
    <!-- LOGGER CONFIGURATION BY PACKAGE -->
    <!-- ============================================ -->

    <!-- Petties Application Loggers -->
    <logger name="com.petties" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC_FILE" />
        <appender-ref ref="ERROR_FILE" />
    </logger>

    <!-- Security Logger (for debugging auth issues) -->
    <logger name="com.petties.petties.security" level="DEBUG" />
    <logger name="com.petties.petties.filter" level="DEBUG" />

    <!-- Service Layer Logger -->
    <logger name="com.petties.petties.service" level="INFO" />

    <!-- Controller Layer Logger -->
    <logger name="com.petties.petties.controller" level="INFO" />

    <!-- Spring Framework Loggers -->
    <logger name="org.springframework.web" level="INFO" />
    <logger name="org.springframework.security" level="WARN" />
    <logger name="org.springframework.data.redis" level="WARN" />

    <!-- Hibernate/JPA Loggers -->
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />
    <logger name="org.hibernate.orm.jdbc.bind" level="TRACE" />

    <!-- Flyway Migration Logger -->
    <logger name="org.flywaydb" level="INFO" />

    <!-- External Libraries -->
    <logger name="com.zaxxer.hikari" level="WARN" />
    <logger name="io.sentry" level="WARN" />
    <logger name="com.cloudinary" level="WARN" />
    <logger name="com.google.firebase" level="WARN" />

    <!-- ============================================ -->
    <!-- PROFILE-SPECIFIC CONFIGURATION -->
    <!-- ============================================ -->

    <!-- Development Profile -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="ASYNC_FILE" />
            <appender-ref ref="ERROR_FILE" />
        </root>

        <!-- Show SQL in dev -->
        <logger name="org.hibernate.SQL" level="DEBUG" />
    </springProfile>

    <!-- Test Profile -->
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>

        <!-- Reduce noise in tests -->
        <logger name="org.hibernate.SQL" level="WARN" />
        <logger name="com.petties" level="INFO" />
    </springProfile>

    <!-- Production Profile -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="ASYNC_FILE" />
            <appender-ref ref="ERROR_FILE" />
            <appender-ref ref="JSON_FILE" />
        </root>

        <!-- Less verbose in production -->
        <logger name="com.petties" level="INFO" />
        <logger name="org.hibernate.SQL" level="WARN" />
        <logger name="org.springframework.security" level="WARN" />
    </springProfile>

    <!-- Default (no profile) -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="ASYNC_FILE" />
        </root>
    </springProfile>

</configuration>