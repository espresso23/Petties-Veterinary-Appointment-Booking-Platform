workflows:
  ios-shorebird-build:
    name: Petties Mobile iOS - Shorebird Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    working_directory: petties_mobile
    environment:
      groups:
        - google-service-account
      flutter: stable
      xcode: latest
      vars:
        ENV: "dev"
        COCOAPODS_DISABLE_STATS: "true"
    scripts:
      - name: Install Shorebird
        script: |
          # Cài Shorebird CLI
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          # Thêm Shorebird vào PATH bằng biến CM_ENV (đúng chuẩn CodeMagic)
          echo "PATH=\"$HOME/.shorebird/bin:$PATH\"" >> $CM_ENV
      - name: Create .env from CodeMagic Variables
        script: |
          echo "ENV=$ENV" > .env
          echo "API_BASE_URL=$API_BASE_URL" >> .env
          cat .env
      - name: Flutter pub get
        script: flutter pub get
      - name: Configure Git and Install Pods
        script: |
          cd ios
          git config --global http.postBuffer 524288000
          git config --global http.lowSpeedLimit 0
          git config --global http.lowSpeedTime 999999
          n=0
          until [ "$n" -ge 3 ]
          do
             pod install && break
             n=$((n+1))
             echo "Pod install failed... retrying ($n/3)"
             sleep 5
          done
      - name: Shorebird Release iOS
        script: shorebird release ios --no-codesign
      - name: Package IPA for Sideloading
        script: |
          rm -rf Payload Runner.ipa
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r Runner.ipa Payload
    artifacts:
      - petties_mobile/Runner.ipa
